#include <stdio.h>

int main(){
  int a = 10 ,  b =500;
  a>b?printf("a is greater than b"):printf("b is greater than a");
  //Here '?' is for if the condition right then frist one apply
  //And ':' is for if the condition not right then second one apply
  return 0;
}